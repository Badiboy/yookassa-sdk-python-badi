language: python
dist: jammy
python:
  - "3.12"
  - "3.11"
  - "3.10"
  - "3.9"
  - "3.8"
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt && pip install -e .
# command to run tests
script:
  - pytest
